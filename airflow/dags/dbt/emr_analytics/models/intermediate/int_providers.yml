models:
  - name: int_providers
    description: Deduplicated provider data, one row per provider
    columns:
      - name: provider_id
        description: Unique identifier for the provider
        data_type: string
        data_tests:
          - not_null
          - unique
